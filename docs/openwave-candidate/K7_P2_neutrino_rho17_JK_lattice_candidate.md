# K7-P2 Candidate A — explicit `rho=17` full-JK lattice candidate

**Status:** `INTEGRAL LATTICE/ACTION PASS / PROJECTIVE TORELLI REALIZATION OPEN`  
**Date:** 2026-09-12  
**Target-value exposure:** none.  
**Parent obstruction:** [`K7_P2_neutrino_JK_character_obstruction.md`](K7_P2_neutrino_JK_character_obstruction.md)

The rank-15 route is impossible for the frozen Joyce–Karigiannis K3 character package. The exact character inversion gives the necessary lower bound `rho >= 17`. This note supplies an explicit integral rank-17 lattice and a commuting `(Z/2)^3` action whose four anti-symplectic invariant lattices match the frozen Nikulin targets exactly.

No neutrino mass, ordering or measured target is used.

---

## 1. Character target at the first feasible rank

Let

`G = <tau, sigma_A, sigma_B> ~= (Z/2)^3`,

with `V4=<sigma_A,sigma_B>` symplectic and the four elements in the `tau` coset anti-symplectic.

At `rho=17`, `rank(T_X)=5`. The frozen fixed-locus character implies

`tr_NS = (17,5,1,1,5,5,1,5)`

in the order

`(1, tau, sigma_A, sigma_B, tau*sigma_A, tau*sigma_B, sigma_A*sigma_B, tau*sigma_A*sigma_B)`.

Fourier inversion gives the rational character multiplicities

- trivial character: `5`;
- `tau`-only character: `0`;
- each of the other six non-trivial characters: `2`.

This is the first Picard rank for which every multiplicity is non-negative.

---

## 2. An integral frame and an explicit code overlattice

Take the even frame

`L0 = U + A1(-1)^15`.

The two dimensions of `U` are assigned to the trivial character. The 15 negative `A1` roots carry

- trivial character `000` with multiplicity `3`;
- each of `010, 001, 110, 101, 011, 111` with multiplicity `2`;
- no `100` (`tau`-only) direction.

An explicit binary `[15,5]` code `C` is generated by the following supports (zero-based):

```text
(3, 6, 11, 12)
(0, 2, 4, 5, 6, 9, 12, 13)
(5, 8, 10, 13)
(0, 1, 2, 3, 5, 6, 8, 14)
(1, 6, 7, 8, 9, 10, 11, 14)
```

Every codeword has weight divisible by four and the code is self-orthogonal. Therefore adjoining the corresponding half-sums of mutually orthogonal `A1` roots gives an **even integral overlattice**

`NS_17(C) / L0 ~= C`

of index `2^5`.

Since `|disc(L0)|=2^15`,

`|disc(NS_17)| = 2^15 / (2^5)^2 = 2^5`.

Moreover

`A_NS ~= C^perp/C ~= (Z/2)^5`.

The dual code contains odd-weight vectors (the reproducer gives one of weight `3`), so `delta=1`. Hence

`NS_17` has signature `(1,16)` and 2-elementary type

`(17,5,1)`.

This type is not fictitious: K3 surfaces with Néron–Severi type `(17,5,1)` are standard in the Nikulin classification, and explicit double-cover models are known. The present construction is a new choice of marked lattice/action inside that isometry class; it is not yet identified with one particular published projective equation.

Public reproducer:

```bash
python3 docs/openwave-candidate/check_k3_rho17_jk_lattice.py
```

---

## 3. Why the four anti-symplectic fixed lattices are exact

For an element `g in G`, let `F_g` be the coordinate subspace spanned by the `A1` roots on which the character value is `+1`.

Each anti-symplectic element fixes exactly nine of the fifteen negative coordinate directions.

The code was selected so that

`dim(C cap F_tau) = 1`,

and the unique non-zero word in this intersection has weight `4`.

Thus the fixed `A1^9` frame is enlarged by exactly one weight-four glue vector. The four involved `A1` roots saturate to `D4`, giving

`NS^tau ~= U + D4(-1) + A1(-1)^5`.

Therefore

`(r,a,delta)_tau = (11,7,1)`.

For each of the three other anti-symplectic elements,

`dim(C cap F_(tau sigma)) = 0`.

No half-sum survives inside the fixed rational subspace, hence

`NS^(tau sigma) ~= U + A1(-1)^9`,

so all three have

`(r,a,delta) = (11,9,1)`.

These are exactly the frozen JK targets. In particular their fixed loci are, by Nikulin's classification,

- `tau`: one genus-2 curve plus two rational curves;
- each `tau sigma`: one elliptic curve plus one rational curve.

This removes the historical T5'' error in which the three `tau sigma` involutions were instead free.

---

## 4. Symplectic `V4` check

Each non-trivial element of `V4` fixes seven negative frame coordinates and has no non-zero codeword supported entirely in its fixed coordinate set.

Therefore

`rk NS^sigma = 2 + 7 = 9`.

For the intended K3 realization the symplectic generators act as `+I` on the rank-5 transcendental lattice, so

`rk H^2(X)^sigma = 9 + 5 = 14`,

which is the invariant rank of a Nikulin symplectic involution. The action on the holomorphic 2-form is then trivial, and the standard theorem gives eight isolated fixed points for each non-trivial element of `V4`.

The full `G`-invariant lattice has rank

`2 + 3 = 5`,

coming from `U` plus the three negative trivial-character directions.

---

## 5. Extension to the K3 lattice: why the discriminant obstruction is gone

The rank-15 construction failed because its attempted `G` character on `T_X` was incompatible with the NS traces. Here the transcendental rank is exactly five and the representation is forced:

- `sigma_A`, `sigma_B` act as `+I` on `T_X`;
- `tau` acts as `-I` on `T_X`.

The discriminant group of `NS_17` is 2-elementary. Every sign change on the `A1` frame acts trivially on `A_NS`, because `-x=x` in a 2-torsion discriminant group. Likewise `-I` on a 2-elementary transcendental discriminant group acts trivially.

Thus the discriminant gluing is **not obstructed by equivariance**: any anti-isometry used to glue `NS_17` to its rank-5 orthogonal complement is automatically compatible with the stated `G` action.

This is the precise feature that was absent from the rank-15 attempt.

A complete public proof should still record an explicit complement / anti-isometry rather than relying only on the standard 2-elementary classification. That is the next lattice-formality subgate.

---

## 6. Relation to known geometry

Two independent literature facts make this candidate plausible rather than merely combinatorial:

1. the Nikulin type `(11,7,1)` is known to coexist with a symplectic involution; Garbagnati–Sarti's order-two criterion gives existence because `7 > 16-11 = 5`;
2. K3 surfaces of Néron–Severi type `(17,5,1)` admit explicit projective / elliptic models, including double covers of `P2` branched over six-line configurations, and van Geemen–Sarti involutions occur naturally on such high-Picard elliptic K3s.

Neither statement alone supplies our complete commuting `V4` plus `tau` package. The code construction above supplies the missing simultaneous lattice action.

---

## 7. What this closes — and what it does not

### Closed at integral lattice level

- the rank-17 NS lattice is explicit;
- its determinant, discriminant length and parity are exact;
- the full `(Z/2)^3` action is commuting by construction;
- `tau` has fixed lattice `(11,7,1)` exactly;
- all three `tau sigma` have fixed lattice `(11,9,1)` exactly;
- the three `V4` elements have the correct invariant rank for symplectic involutions;
- the rank-15 negative-character obstruction is gone;
- the full-group invariant lattice has rank 5.

### Still open

1. **Explicit K3-lattice gluing certificate.** Give the rank-5 transcendental lattice and an explicit discriminant anti-isometry, then identify the glued even unimodular lattice with `Lambda_K3`.
2. **Global Torelli / ample chamber.** Exhibit a `G`-stable ample chamber and promote the Hodge isometries to actual automorphisms on one projective K3.
3. **Degree-8 model.** Find a primitive `G`-invariant ample class `h` with `h^2=8`, so the same K3 is realized as the required genus-5 `CI(2,2,2)` rather than merely some projective K3.
4. **Elliptic fiber.** The invariant lattice has signature `(1,4)` and rank 5, so Meyer guarantees an integral isotropic vector at lattice level. It must still be made primitive and nef in the selected `G`-stable chamber, with section data controlled.
5. **Global K7 step.** Show the JK resolution / matching preserves that elliptic datum and only then return to the heterotic-duality gate.

No neutrino operator has yet been derived.

---

## 8. Current ruling

> **The full frozen JK fixed-locus package is no longer blocked at the K3 lattice level. There exists an explicit rank-17 even integral candidate carrying the required `(Z/2)^3` character and the exact four anti-symplectic Nikulin invariant lattices.**

This upgrades H0a.3 from a rank-15 `FAIL` to a **new rank-17 constructive route**, but it is not yet a projective-geometric pass.
